name: Deploy static content to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4  # Updated to latest

      - name: Setup Pages
        uses: actions/configure-pages@v4  # Updated to latest

      - name: Debug - List files
        run: ls -la  # Check if static files exist

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2  # Corrected version
        with:
          path: './'  # Update this to match where your static files are

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4  # Updated to latest
